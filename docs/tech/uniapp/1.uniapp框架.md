官网： [https://uniapp.dcloud.net.cn/](https://uniapp.dcloud.net.cn/)
# 1. 什么是uniapp

- uni-app 是一个使用 [Vue.js](https://vuejs.org/) 开发所有前端应用的框架，开发者编写一套代码，可发布到iOS、Android、Web（响应式）、以及各种小程序（微信/支付宝/百度/头条/飞书/QQ/快手/钉钉/淘宝）、快应用等多个平台。
- uni-app在手，做啥都不愁。即使不跨端，uni-app也是更好的小程序开发框架（[详见](https://ask.dcloud.net.cn/article/35947)）、更好的App跨平台框架、更方便的H5开发框架。不管领导安排什么样的项目，你都可以快速交付，不需要转换开发思维、不需要更改开发习惯。
- 简单来说： 开发一次，多端覆盖
# 2.基本语言和开发规范
[https://uniapp.dcloud.net.cn/tutorial/](https://uniapp.dcloud.net.cn/tutorial/)<br />为了实现多端兼容，综合考虑编译速度、运行性能等因素，uni-app 约定了如下开发规范：

- 页面文件遵循 [Vue 单文件组件 (SFC) 规范](https://vue-loader.vuejs.org/zh/spec.html)，即每个页面是一个.vue文件
- 组件标签靠近小程序规范，详见[uni-app 组件规范](https://uniapp.dcloud.net.cn/component/)
- 接口能力（JS API）靠近小程序规范，但需将前缀 wx、my 等替换为 uni，详见[uni-app接口规范](https://uniapp.dcloud.net.cn/api/)
- 数据绑定及事件处理同 Vue.js 规范，同时补充了[应用生命周期](https://uniapp.dcloud.net.cn/collocation/App.html#applifecycle)及[页面的生命周期](https://uniapp.dcloud.net.cn/tutorial/page.html#lifecycle)
- 如需兼容app-nvue平台，建议使用flex布局进行开发
# 3.下载HBuilder编辑器
[https://hx.dcloud.net.cn/Tutorial/install/windows](https://hx.dcloud.net.cn/Tutorial/install/windows)
# 4.创建项目
1.【文件】【新建】【项目】选择【uniapp项目】-------------选择vue3版本
```javascript
// -----------------------------main.js

import App from './App'

// #ifndef VUE3 不是vue3即vue2时运行代码
import Vue from 'vue'
import './uni.promisify.adaptor'
Vue.config.productionTip = false
App.mpType = 'app'
const app = new Vue({
  ...App
})
app.$mount()
// #endif

// #ifdef VUE3  vue3时运行代码
import { createSSRApp } from 'vue'
export function createApp() {
  const app = createSSRApp(App)
  return {
    app
  }
}
// #endif
```
2.【运行】-【运行到浏览器】或【运行到内置浏览器】

# 5. 如何运行在微信开发者工具

- 运行-> 运行到小程序模拟器 -> 微信开发者工具
- 在弹窗中要定位开发者工具的安装路径：常规安装位置 c:/Program Files/Tencent/微信web开发者工具
- 在微信开发者工具管理项目的界面 -> 设置 -> 安全 -> 开启服务端口<br />如果没有开启服务端口，在Hbuiler编译界面会报错：Error: read EBADF
- 在manifest.json中填写微信小程序的appid<br />如果没有填写，在微信开发者工具中会报错：TypeError: Cannot read property 'forceUpdate' of undefined
# 6. 创建新页面
[https://uniapp.dcloud.net.cn/collocation/pages.html](https://uniapp.dcloud.net.cn/collocation/pages.html)
## 回顾小程序
在app.json中配置pages,即会在pages文件夹中创建相应的文件
## uniapp
1. 在pages文件夹上右键，新建页面，会同步的在pages.json中自动生成对应的配置<br />2.在新建页面，可以点击下方的【自定义模板】，把模板文件粘贴到打开的文件夹<br />3.再次新建时，即可看到自定义模板<br />4.pages.json中pages的配置相当于路由，所有页面都需要在此注册
```json
{
	"pages": [ //pages数组中第一项表示应用启动页，参考：https://uniapp.dcloud.io/collocation/pages
		{
			"path": "pages/index/index",
			"style": {
				"navigationBarTitleText": "uni-app"
			}
		}
	],
	"globalStyle": {
		"navigationBarTextStyle": "black",
		"navigationBarTitleText": "uni-app",
		"navigationBarBackgroundColor": "#F8F8F8",
		"backgroundColor": "#F8F8F8"
	},
	"uniIdRouter": {}
}

```
# 7. 页面导航栏标题文本，背景颜色，下拉刷新等相关配置
小程序： 在app.json中全局配置，在每个页面的json文件中给当前页面配置<br />uniapp: 全局和每个页面的配置都在pages.json中实现
# 8.tabBar的配置
## static文件夹
> 本地静态资源，比如图片，图标文件一定要放在static文件夹中，引用地址的方式参考下方代码

![image.png](https://cdn.nlark.com/yuque/0/2023/png/8438658/1692614541015-d1b1c93c-3fdb-45ae-a249-b47304ba3cf9.png#averageHue=%23292e37&clientId=ua3070789-0732-4&from=paste&height=209&id=uc1b4a153&originHeight=365&originWidth=1285&originalType=binary&ratio=1.75&rotation=0&showTitle=false&size=58209&status=done&style=none&taskId=u4ad128d9-3318-413e-8128-eb2f37c5f8c&title=&width=734.2857142857143)<br />static目录 使用注意

- 编译到任意平台时，static 目录下除不满足[条件编译](https://uniapp.dcloud.net.cn/tutorial/platform.html#static-%E7%9B%AE%E5%BD%95%E7%9A%84%E6%9D%A1%E4%BB%B6%E7%BC%96%E8%AF%91)的文件，会直接复制到最终的打包目录，不会打包编译。非 static 目录下的文件（vue、js、css 等）只有被引用时，才会被打包编译。
- css、less/scss 等资源不要放在 static 目录下，建议这些公用的资源放在自建的 common 目录下。
## tabBar配置
在package.json中配置
```json
"tabBar": {
  "color": "#567788",
  "selectedColor": "#f96677",
  "list": [
    {
      "pagePath": "pages/index/index",
      "text": "首页",
      "iconPath": "/static/images/icon0.png",
      "selectedIconPath": "/static/images/icon1.png"
    },
    {
      "pagePath": "pages/category/category",
      "text": "分类",
      "iconPath": "/static/images/icon2.png",
      "selectedIconPath": "/static/images/icon3.png"
    }
  ]
}
```
# 9.页面生命周期
[https://uniapp.dcloud.net.cn/tutorial/](https://uniapp.dcloud.net.cn/tutorial/)  【教程】【页面】【生命周期】<br />uni-app 页面除支持 Vue 组件生命周期外还支持下方页面生命周期函数，当以组合式 API 使用时，在 Vue2 和 Vue3 中存在一定区别，请分别参考：[Vue2 组合式 API 使用文档](https://uniapp.dcloud.net.cn/tutorial/vue-composition-api.html) 和 [Vue3 组合式 API 使用文档](https://uniapp.dcloud.net.cn/tutorial/vue3-composition-api.html)。

## 常用页面生命周期
![image.png](https://cdn.nlark.com/yuque/0/2023/png/8438658/1692614093275-361c6ecc-2026-4b61-a2b8-751a0d546669.png#averageHue=%23ebefd3&clientId=ua3070789-0732-4&from=paste&height=471&id=u9592e2c9&originHeight=825&originWidth=1818&originalType=binary&ratio=1.75&rotation=0&showTitle=false&size=212512&status=done&style=none&taskId=u8d2d0863-1dd1-4c0b-928d-bf32dff040b&title=&width=1038.857142857143)
## 使用组合式API
从 vue 包内导入并使用基础的组合式API，具体 API 可以参考：[Vue 官网](https://cn.vuejs.org/api/composition-api-setup.html)。从 @dcloudio/uni-app 包内导入 uni-app [应用生命周期](https://uniapp.dcloud.net.cn/collocation/App.html#applifecycle)及[页面的生命周期](https://uniapp.dcloud.net.cn/tutorial/page.html#lifecycle)。
```javascript
<script setup>
import { ref } from 'vue'
import { onReady } from '@dcloudio/uni-app'
const title = ref('Hello')
onReady(() => {
  console.log('onReady')
})
</script>
```
# 10. 在项目中使用sass
新建页面时选择sass类型
```html
<style lang="scss">
</style>
```

- 如果没有安装sass,在编译时会报错 （此时HBuilder会自动安装sass预编译器）
- 项目初始化时创建的uni.scss文件不用导入，定义的变量可直接在任一页面中使用
- 在App.vue的style中可以 设置每个页面公共css
- 引入scss和css文件的语法

```html
<style lang="scss">
  @import  "./var.scss";
  @import "./header.css";
  @import '@/scss/common.scss';
</style>
```
> 注意： 不要写成  [@import ](/import) url(

# 11. 条件编译和判断平台
[https://uniapp.dcloud.net.cn/tutorial/platform.html#%E8%B7%A8%E7%AB%AF%E5%85%BC%E5%AE%B9](https://uniapp.dcloud.net.cn/tutorial/platform.html#%E8%B7%A8%E7%AB%AF%E5%85%BC%E5%AE%B9)

## 条件编译
是用特殊的注释作为标记，在编译时根据这些特殊的注释，将注释里面的代码编译到不同平台。
## 编译期判断
 编译期判断，即条件编译，不同平台在编译出包后已经是不同的代码。

- 在 *.vue文件中，template,script,style中都可以添加条件编译，注意在不同区块中注释的写法是不同的

```html
<!-- #ifdef H5 -->
<view>当前城市： {{city}}</view>
<!-- #endif  -->

<!-- #ifdef MP-WEIXIN -->
<view>当前温度： 18度</view>
<!-- #endif  -->

<!-- #ifdef APP-PLUS -->
<view>当前温度： 18度</view>
<!-- #endif  -->
```

```vue
<template>
  <view class="content">
    <!-- #ifdef H5 -->
    <view class="box1">
      H5---{{title}}
    </view>
    <!-- #endif -->
    
    <!-- #ifdef MP-WEIXIN -->
    <view class="box1">
      微信小程序--{{title}}
    </view>
    <!-- #endif -->
    
  </view>
</template>

<script>
  export default {
    data() {
      return {
        // #ifdef H5
        title: 'Hello-h5',
        // #endif
        
        // #ifdef MP-WEIXIN
        title: 'Hello-mp',
        // #endif
      }
    },
    onLoad() {

    },
    methods: {

    }
  }
</script>

<style>
  .content {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }
  .box1 {
    font-size: 60rpx;
    /* #ifdef H5 */
    border: 2rpx solid #f00;
    /* #endif */
    
    /* #ifdef MP-WEIXIN */
    border: 2rpx solid #458833;
    /* #endif */
    
    width: 700rpx;
    height: 200rpx;
  }

  .logo {
    height: 200rpx;
    width: 200rpx;
    margin-top: 200rpx;
    margin-left: auto;
    margin-right: auto;
    margin-bottom: 50rpx;
  }

  .text-area {
    display: flex;
    justify-content: center;
  }

  .title {
    font-size: 36rpx;
    color: #8f8f94;
  }
</style>
```

## 运行期判断 
运行期判断是指代码已经打入包中，仍然需要在运行期判断平台
> 只能区别机型（ios,Android）

```javascript
switch(uni.getSystemInfoSync().platform){  //获取App运行的客户端平台
  case 'android':
    console.log('运行Android上')
    break;
  case 'ios':
    console.log('运行iOS上')
    break;
  default:
    console.log('运行在开发者工具上')
    break;
}
```
# 12. CSS变量
uni-app 提供内置 CSS 变量

| CSS 变量 | 描述 | App | 小程序 | H5 |
| --- | --- | --- | --- | --- |
| --status-bar-height | 系统状态栏高度 | [系统状态栏高度](http://www.html5plus.org/doc/zh_cn/navigator.html#plus.navigator.getStatusbarHeight)<br />、nvue 注意见下 | 25px | 0 |
| --window-top | 内容区域距离顶部的距离 | 0 | 0 | NavigationBar 的高度 |
| --window-bottom | 内容区域距离底部的距离 | 0 | 0 | TabBar 的高度 |

比如：自定义导航栏时，要空出状态栏的位置，可添加一个空的view进行占位
```html
<view>
  <view class="header"></view>
  <view>list文本内容</view>
</view>

<style>
  .header {
    height: var(--status-bar-height);
    width: 100%;
    border: 1px solid #007AFF ;
  }
</style>
```
# 13. CSS单位
rpx
# 14. 在模板中vue和小程序的区别
## 1. 动态属性

```html
//数据 url="/images/1.jpg"

//uniapp中的vue
<image :src="'http://m.mi.com' + url"></image>
<image :src="`http://m.mi.com${url}`"></image>

//小程序: 动态属性绝对不加: ,""内部为字符串，需要绑定数据时加{{}}

<image src="http://m.mi.com{{url}}"></image>
```
## 2. 标签内容显示相同
```html
//数据：  msg="hello"
<view>{{msg}}</view>
```
## 3.  循环遍历
```html
// vue
<view v-for="(item,index) in list"></view>

//小程序
<view wx:for="{{list}}"></view>
```
## 4. 条件渲染
```html
// vue
<view v-if="true"></view>

//小程序
<view wx:if="{{true}}"></view>
```
# 15. 用navigator组件进行跳转

```html
<view class="container">
  <navigator class="item" v-for="(item,index) in list" :key="index" :url="`/pages/detail/detail?id=${item.id}`">
    <view>id: {{item.id}}</view>
    <view>goodsName: {{item.goods_name}}</view>
  </navigator>
</view>
```

> 也可以用uni.navigateTo跳转传参， 在下一个页面中，通过onLoad(options)接收参数

# 16. 事件绑定中的传参
uniapp传参
```html
<view @click="getData(3)"></view>
...

getData(params){
  console.log(params)  //3
}
```

小程序传参
```html
<view bindtap="getData" data-index="3"></view>
...

getData(e){
console.log(e.currentTarget.dataset.index)  //3
}
```
# 17.网络请求
## uni.request

vue3

```js
// promise/then版本
const getData =()=>{
	uni.request({
		url: '/api/list'
	})
	.then(res=>{
		console.log(res);
	})
}

// async和await版本
const getData = async ()=>{
	let res = await uni.request({
		url: '/api/list'
	})
	
	console.log(res);
}

onLoad(()=>{
	getData()
})
```

vue2 不支持promise

```javascript
import {ref,onMounted} from 'vue'
	const list = ref([])
	
	const getData =  () => {
    //请求数据
		uni.request({
			url:'/api/user',
			success(res) {
				list.value = res.data.list
			}
		})
	}
	onMounted(()=>{
		getData()
	})
```
## H5跨域
在根目录新建vite.config.js
```javascript
import { defineConfig } from 'vite';
//引入uni模块
import uni from '@dcloudio/vite-plugin-uni';

export default defineConfig({
	plugins: [uni()],
	server: {
		proxy: {
			"/api": {
				target: "http://localhost:5000",
				changeOrigin: true,
				rewrite: (path) => path.replace(/^\/api/, ""),
			},
		},
	},
});

```
# 18.自定义组件
## easycom组件规范
【全局文件】【pages.json页面路由】【easycom】

传统vue组件，需要安装、引用、注册，三个步骤后才能使用组件。easycom将其精简为一步。<br />只要组件路径符合规范（具体见下），就可以不用引用、注册，直接在页面中使用。

路径规范指：

1. 安装在项目根目录的components目录下，并符合components/组件名称/组件名称.vue
2. 安装在uni_modules下，路径为uni_modules/插件ID/components/组件名称/组件名称.vue
## 父向子传值

## 子向父传值

# 19.登录/支付

适配微信小程序的登录

```js
uni.login({
  provider: 'weixin', //使用微信登录
  success: function (loginRes) {
    console.log(loginRes.authResult);
  }
});
```

适配微信支付

```js
uni.requestPayment({
    "provider": "wxpay", 
    "orderInfo": {
        "appid": "wx499********7c70e",  // 微信开放平台 - 应用 - AppId，注意和微信小程序、公众号 AppId 可能不一致
        "noncestr": "c5sEwbaNPiXAF3iv", // 随机字符串
        "package": "Sign=WXPay",        // 固定值
        "partnerid": "148*****52",      // 微信支付商户号
        "prepayid": "wx202254********************fbe90000", // 统一下单订单号 
        "timestamp": 1597935292,        // 时间戳（单位：秒）
        "sign": "A842B45937F6EFF60DEC7A2EAA52D5A0" // 签名，这里用的 MD5/RSA 签名
    },
    success(res) {},
    fail(e) {}
})
```

