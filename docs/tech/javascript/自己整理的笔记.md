---
layoutClass: m-nav-layout
---

1.dom的基础语法

### 1.1查找 HTML 元素

```js
//通过id查询
document.getElementById(id)	通过元素 id 来查找元素。
```

```js
//通过类名查询
document.getElementsByClassName(name)	通过类名来查找元素。
```

```js
//通过标签查找
document.getElementsByTagName(name)	通过标签名来查找元素。
```

```js
//通过选择器查找一个元素
document.querySelector(CSS选择器)	通过CSS选择器选择一个元素。
```

```js
//通过css选择多个元素
document.querySelectorAll(CSS选择器)	通过CSS选择器选择多个元素。
```

```js
//查看获取对象的属性
console.log([对象])
```

### 1.2获取html的值

```js
//元素节点.innerText	获取 HTML 元素的内容。
```

```js
//元素节点.innerHTML	获取 HTML 元素的内容和标签。
```

```js
//元素节点.属性	获取 HTML 元素的属性值。
```

```js
//元素节点.getAttribute(attribute)	获取 HTML 元素的属性值。
```

```js
//元素节点.style.样式	获取 HTML 元素的行内样式值。
```

```js
//获取多个元素的时候，获取的是集合，伪数组，不能给集合添加内容
//使用循环可以一次给集合添加内容，伪数组可以使用forEach
```

#### 1.2.1 访问元素的原生属性

```js
//<div class="a1" id="a2"></div>
//获取dom对象的原生属性
let div = document.queryselector('div')
console.log(div.className) //打印出div的class属性
console.log(div.id)
```

```js
//通过访问图片的src改变img的照片显示
// <img src="./o1.ipg" alt="" />
let img = document.queryselector('img')
img.src = './02.jpg'
```

```
//给图片设置点击事件进行切换img的src的路径
img addEventListener('click',function(){
  img.src = './02.jpg'
})
```

#### 1.2.2 设置元素的自定义属性

```js
//设置dom对象的自定义属性的三种方法  方法一：
//  <div class="box"></div>
box.setAttribute('color', 'red') //创建自定义属性
console.log(box.getAttribute('color')) //访问自定义属性
box.removeAttribute('color') //删除自定义属性
console.log(box.getAttribute('color')) //打印结果为空
```

```js
//  方法二
//<div class="box" data-color="blue">1111</div>
//  直接在标签的选择器位置加上 date-属性 = ''   添加自定义属性
let box = document.querySelector('.box')
console.log(box.dataset.color) //访问自定义属性
```

```js
//方法三
let box = document.querySelector('.box')
box.type = ' color' //给dom添加属性，
console.log([box]) //查看属性释放添加上属性
console.log(box.type) //访问属性
```

#### 1.2.3设置dom元素的样式

```
1.css样式的属性值一定是字符串
2.复合属性例如background-color在js中要用驼峰命名 backgroundColor
3.dom.style.属性名只能获取行内样式
4.实际开发往往先设置行内样式，再根据业务逻辑获取样式的值
```

```html
<div class="box">22222</div>
```

```js
let box = document.querySelector('.box')
box.style.width = '400px' //设置元素的样式
console.log(box.style.width) //得到的属性值是字符串
//如果使用Number直接进行转化，会得到NAN
//例如：console.log(Number(box.style.width))
console.log(parseInt(box.style.width)) //字符串转换为Number
```

### 1.3 判断是不是数组

```js
//方法一  使用 instanceof方法检测
const arr = [1, 2, 3, 4, 56]
//通过instanceof 方法判断是否arr是不是数组，如果是返回ture,如果不是返回flase
const isArr = arr instanceof Array
console.log(isArr)
```

```js
//方法二  使用数组的方法 Array.isArray(需要检测的数组)
//如果是返回ture,如果不是返回flase
const arr = [1, 2, 3, 4, 56]
const newArr = Array.isArray(arr)
console.log(newArr2)
```

### 1.4 控制集合中的dom元素

```js
//集合是一个伪数组，可以使用for循环和forEach循环
//------方法1--------
//---------box得到所有li的集合,集合的话是一个伪数组，可以访问伪数组的长度
//---------通过for循环给每个li添加内容
for (let i = 0; i < box.length; i++) {
  box[i].innerText = '你好'
}
```

```js
//------方法2--------
//--------通过forEach为每个li添加内容
box.forEach((element) => {
  element.innerText = '你好2'
})
```

## 2.事件监听

```
1.鼠标事件
click鼠标点击  mouseenter 鼠标经过 mouseleave 鼠标离开
2.焦点事件 focuns 获得焦点 blur失去焦点
3.键盘事件 keydown 键盘按下 keyup键盘抬起触发
4.文本事件-表单输入触发 input
```

#### L0版本的事件监听

```
//窗口事件
onblur	当窗口失去焦点时运行脚本。
onfocus	当窗口获得焦点时运行脚本。
onload	当文档加载之后运行脚本。
```

```
//表单事件
```

#### 2.2 L2版本addeventlistener

```
click事件：当用户点击元素时触发，适用于按钮、链接、图片等元素。

mouseover事件：当鼠标移动到元素上时触发，适用于提示信息、下拉菜单等元素。 mouseenter

mouseout事件：当鼠标移出元素时触发，适用于提示信息、下拉菜单等元素。mouseleave

keydown事件：当用户按下键盘上的任意键时触发，适用于表单输入、游戏等需要键盘交互的场合。

blur	当窗口失去焦点时运行脚本。
focus	当窗口获得焦点时运行脚本。
input   文本事件
```

## 3.表单元素

### 3.1\*\*\*访问表单元素

```js
//--------- 设置和访问-----文本框的值-------
const text = document.querySelector('.text')
text.value = '1111'
console.log(text.value)
```

```js
//---------设置和访问--------文本域的值------
const textarea = document.querySelector('textarea')
textarea.value = '你好我是文本域的内容'
console.log(textarea.value)
```

```js
// ---------设置和访问--------单选按钮的值------
const ganderList = document.querySelectorAll('.gander input')
//设置两个单选框的值
for (let i = 0; i < ganderList.length; i++) {
  ganderList[i].value = 1
}
console.log(ganderList[0].value)
console.log(ganderList[1].value)
```

```js
//------------单选框的默认值-------------
const ganderList = document.querySelectorAll('.gander input')
const btn = document.querySelector('.gander button')
btn.onclick = function () {
  //   console.log(ganderList[0].checked);
  //   ganderList.forEach(element => {
  //         if(ganderList[element]===)
  //   });
  for (let i = 0; i < ganderList.length; i++) {
    if (ganderList[i].checked) {
      console.log(ganderList[i].checked)
    }
  }
}
```

```html
-----------------单选框的html文件---------
<p class="gander">
  请选择您的性别：
  <input type="radio" name="gander" value="1" />
  男
  <input type="radio" name="gander" value="2" />
  女
  <button>
    选中的内容是：
    <span class="span1"></span>
  </button>
</p>
```

```js
// --------单选框选中时候获取选中单选框的值-------
const ganderList = document.querySelectorAll('.gander input')
const btn = document.querySelector('.gander button')
const span = document.querySelector('.gander .span1')
const arr = ['男', '女']
btn.onclick = function () {
  for (let i = 0; i < ganderList.length; i++) {
    if (ganderList[i].checked) {
      console.log((span.innerText = arr[i]))
      break
    }
  }
}
```

```js
//多选框	const aaa = document.querySelectorAll(".checkbox input");
const btn2 = document.querySelector('.checkbox button')
const span2 = document.querySelector('.checkbox .bbb')
let str = ''
console.log(aaa)
const arr2 = ['篮球', '足球', '羽毛球']
btn2.onclick = function () {
  for (let i = 0; i < aaa.length; i++) {
    if (aaa[i].checked) {
      str = str + arr2[i]
    }
  }
  span2.innerText = str
}
```

```html
---------------下拉列表的html文件-------------
<p>
  城市：
  <select name="" id="" class="city">
    <option value="北京">北京</option>
    <option value="上海">上海</option>
    <option value="深圳">深圳</option>
    <option value="广州">广州</option>
  </select>
</p>
```

```js
//-----下拉列表的值有变化的时候，打印出变化的值---------
//也可以添加绑定事件，点击按钮获取下拉列表的值
const select = document.querySelector('.city')
aaa.onchange = function () {
  console.log(select.value)
}
```

### 3.2 \*\*\*模仿购物车的全选效果

```
1.全选的复选按钮不能和其他的复选按钮放到一起
2.复选框勾选的时候返回的值是ture,不勾选的时候返回的值是flase
3.分析全选复选按钮勾选时候和其他复选按钮勾选时候的关系
```

```html
<p>
  <input type="checkbox" class="quanxuan" />
  全选
</p>
<p class="hobby">
  <input type="checkbox" />
  吃
  <input type="checkbox" />
  喝
  <input type="checkbox" />
  玩
  <input type="checkbox" />
  乐
</p>
```

```js
//实现勾选复选按钮时候，其他复选按钮也被勾选
//获取全选按钮的值，获取其他按钮的集合
let quanxuan = document.queryselector('.quanxuan')
let hobby = document.queryselectorall('.hobby input')
hobby.onclick = function () {
  for (let i = 0; i < hobby.length; i++) {
    hobby[i].checked = quanxuan.checked
  }
}
```

```js
//分析1.实现勾选其他全部按钮的时候全选按钮才会被勾选
//2.技巧勾选一个按钮，设置一个值为1，全部勾选时候值为4，
//值为4的时候，全选按钮才会被勾选上
//给每个按钮都设置点击事件
for (let i = 0; i < hobby.length; i++) {
  hobby[i].onclick = function () {
    //设置一个变量记录当前选中了几个按钮
    let count = 0
    for (let i = 0; i < hobby.length; i++) {
      if (hobby[i].checked) {
        count++
      }
    }
    //当四个全部勾中的时候全选按钮才会被勾中
    quanxuan.checked = count === 4
  }
}
```

## 6.es6新增

6.1 声明变量

```
var let const 的区别
1. var声明变量存在变量提升，let和const不存在变量提升
2. let和const只能在块作用域里访问
3. 同一作用域下let和const不能声明同名变量，而var可以
4. const定义常量，而且不能修改，但是在定义的对象时对象属性值可以改变
```

## 7.箭头函数

7.1 箭头函数可以简写

```js
普通函数
let sum = function () {
  console.log('你好1')
}
sum()
```

```js
箭头函数
let sum = () => {
  console.log('你好2')
}
sum()
```

```
有返回值的普通函数
let sum = function(a,b){
    return a+b
}
sum(1,2)
```

```
有返回值的箭头函数
let sum = ()=> a+b   //只有一行时候return可以省略不写
sum(1,2)
```

7.2 箭头函数和普通函数的区别

```
箭头函数实现了一种更加简洁的书写方式。箭头函数内部没有arguments，也没有prototype属性，prototype(暂时不会)
```

```js
普通函数
let num = function (a, b) {
  console.log(arguments)
}
num(1, 2, 3)
```

```js
箭头函数
let num = (a, b) => {
  console.log(arguments) //报错
}
num(1, 2)
```

```
箭头函数的this指向的是全局对象window
```

```
普通函数
var age = 123;
let obj = {
	age: 456,
	say: () => {
		console.log(this.age); //this指向window全局对象
	},
};
obj.say(); //123
```

```
箭头函数
var age = 123;
let obj = {
	age: 456,
	say: function () {
		console.log(this.age); //this指向的是当前的作用域obj的age456
	},
};
obj.say(); //456
```

```
事件绑定中this指向
  <div class="box" data-type="car">box</div>
```

```js
普通函数
let box3 = document.querySelector('.box')
box3.onclick = function () {
  // 事件处理程序内部的this,指向触发当前事件的dom对象
  console.log('this', this) //指向box3
  var type = this.dataset.type
  console.log(type)
}
```

```js
箭头函数
console.log(this) // window
let box3 = document.querySelector('.box')
box3.onclick = () => {
  // 事件绑定中不要使用箭头函数
  // 箭头函数内部的this,指向定义该函数的环境的this
  console.log('this', this) // this是全局的window对象
  // 等价于window.dataset.type  等价于访问undefined.type
  var type = this.dataset.type
  console.log(type)
}
```

```
事件绑定中的this指向是window
```

```
1.间歇性函数普通函数this指向是window
2.间歇性函数箭头函数this指向是window
3.间歇性函数箭头this指向放在事件绑定中是dow对象
```

## 1.轮播图淡入淡出

```
1.需要把父盒子设置相对定位
2.子盒子绝对定位
3.所有的子元素隐藏
4.设置显示的类名
5.设置定时器
6.移除类名，设置自增
7.当等于多少的时候把第一个元素开启显示并且归0
8.不等于多少的时候。当前元素显示
```

## 2.轮播图的切换

```
1.先设置盒子长宽设置超出隐藏
2.ul设置flex
3.设置类名过度属性
3..获取ul的节点
4.设置定时器
5.自增
6.设置dom的样式 添加过度属性
7.设置监听事件transitionend 当自增到多少的时候移除过度属性，设置样式为transition为0
自增设为0


```

## 一.获取元素常见的尺寸

```
1.box.style.属性获取的行内式，不能获取到style里面的样式，需要先加属性再获取

2.getcomputed获取元素的最终样式

3.box.clintWidth直接取整number

2.开发中获取样式

2.1

box.clientWidth获取宽度

box.clientHeight获取高度

2.2

dom.offsetWidth

dom.offsetHeight

2.3

offsetLeft

offsetTop

```

2. 4

1.导航栏的显示与隐藏

```
1.先设置隐藏导航栏，导航栏设置固定定位
2.设置内容，内容的高度大于父盒子的时候，父盒子设置overflow:auto就会显示滑动
3.获取大盒子的dom节点，还有导航栏的dom节点
3.监听大盒子的滑动，scroll，当滑动的距离顶部大于100时候导航栏显示小于100时候导航栏消失
```

2. 滑块的设计

```
1.获取ul里面所有的li
2.获取装内容的所有盒子节点
3.遍历ul,给每个li设置点击事件
4.设置滑动的方式be:sm，设置点击li时候滑动的位置，
位置的话是通过offsetTop来确定的，
```

2。window版

```
1.给window设置监听事件
2.获取window的滚动时候内卷的值
```

## 一.移动端事件

```html
touchstart便是触摸的时候 touchmove移动 touchend 触摸结束 click 单击（移动端点击延迟）
解决单击延迟fastclick快速点击解决300ms延迟问题 404代表的地址错误
```

```
//1.touchstart
box.addEventListener('touchstart',function(){

})
```

## 事件冒泡\*\*\*

```
在标准的事件流中，当我们触发一个元素的事件时，会经历三个阶段：

1. 捕获阶段，从document沿着DOM树向下捕获， 直到找到这个目标元素位置，
2. 目标阶段： 如果目标元素绑定的有该事件，则执行
3. 冒泡阶段： 之后从这个元素沿着DOM树向上冒泡，有相同事件的元素会执行对应元素上的事件，没有则继续冒泡，直到到document结束
```

## 事件委托\*\*\*

```
事件代理”即是把原本需要绑定在子元素的响应事件委托给父元素或祖先元素，让父元素或祖先元素担当事件监听的职务。事件代理即是利用事件冒泡的机制把里层所需要响应的事件绑定到外层

事件委托的优点

- 可以大量节省内存占用，减少事件注册
- 可以实现当新增子对象时无需再次对其绑定
```

```
祖先元素.on事件类型 = function (e) {
  if (e.target.xxx === "xxx") {
    // 执行对应的操作
  }
}
```

## 状态码\*\*\*

403表示没有权限拒绝访问

404表示地址错误

## 图片懒加载\*\*\*

### 图片懒加载的应用场景

懒加载(Lazy-Load)。它是针对图片加载时机的优化：在一些图片量比较大的网站（比如电商网站首页，或者团购网站、小游戏首页等），如果我们尝试在用户打开页面的时候，就把所有的图片资源加载完毕，那么很可能会造成白屏、卡顿等现象，因为图片真的太多了，一口气处理这么多任务，浏览器做不到啊

### 图片懒加载的目的

懒加载 的目的是当页面的图片进入到用户的可视范围之内在加载图片的一种优化方式。

可以增加首屏加载的速度，毕竟，用户点开页面的瞬间，呈现给他的只是首屏，我们只要把首屏的资源图片加载处理就可以了，至于下面的图片，当用户下滑到当前位置的时候，再加载出来也是没问题的，对于性能压力也小了，用户体验也没有变差

### 图片懒加载的实现原理

- 图片的加载是由src 引起的，当对src 赋值时，浏览器就会请求图片资源。根据这个原理，我们使用HTML5 的data-xxx 属性来储存图片的路径，在需要加载图片的时候，data-xxx 中图片的路径赋值给src，这样就实现了图片的按需加载，即懒加载。
- 注意：data-xxx 中的xxx 可以自定义，这里我们使用data-src 来定义。
- 懒加载的实现重点在于确定用户需要加载哪张图片，在浏览器中，可视区域内的资源就是用户需要的资源。所以当图片出现在可视区域时，获取图片的真实地址并赋值给图片即可

### 代码实现

```
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- 破解图片防盗链 -->
    <meta name="referrer" content="no-referrer" />
    <title>Document</title>
    <style>
      * {
        margin: 0;
        padding: 0;
      }
      .list img {
        width: 160px;
        height: 160px;
        border: 1px solid #000;
      }
    </style>
  </head>
  <body>
    <div class="list">
      <ul></ul>
    </div>
    <script src="./js/flowerData.js"></script>
    <script>
      //获取窗口的高度
      let winHeight = window.innerHeight;
      let ulEl = document.querySelector(".list ul");

      //初始化渲染所有的商品信息
      function init() {
        let html = "";
        flowerData.forEach((item) => {
          html += `
            <li>
                <a href="#">
                    <p>名称:${item.name}</p>
                    <p><img src="./img/img-placeholder.png" alt="" data-src="${item.pic}" class="lazy"></p>
                </a>
            </li>

        `;
        });
        ulEl.innerHTML = html;
      }

      //初始化显示已经在可视窗口中的图片
      function lazyLoad() {
        let imgList = document.querySelectorAll(".lazy");
        imgList.forEach((pic) => {
          //获取当前图片距离可视窗口顶端的偏移值
          let offsetTop = pic.getBoundingClientRect().top;
          if (offsetTop <= winHeight) {
            //说明图片已经在可视窗口
            pic.src = pic.dataset.src;
            pic.classList.remove("lazy")
          }
        });
      }
      init();
      lazyLoad();
      window.addEventListener("scroll", lazyLoad);
    </script>
  </body>
</html>

```
