# 1.了解Content-Type属性
Content-Type，内容类型，一般是指网页中存在的Content-Type，用于定义网络文件的类型和网页的编码，决定浏览器将以什么形式、什么编码读取这个文件，
# 2.了解Content-Type四种常见取值
## 1.application/x-www-form-urlencoded 
最常见 POST 提交数据的方式。浏览器的原生 form 表单，如果不设置 enctype 属性，那么最终就会以 application/x-www-form-urlencoded 方式提交数据。
## 2.multipart/form-data 
这是另一种非常常见的 POST 数据提交的方式。我们在使用表单上传文件时，必须让 form 的 enctyped 等于这个值。
## 3.application/json 
application/json 关于这个 Content-Type 作为响应头大家应该并不会感到不陌生。实际上，现在越来越多的人把它作为请求头，用来告诉服务端消息主体是序列化后的 JSON 字符串。由于 JSON 规范的流行，除了低版本 IE 之外的各大浏览器都原生支持 JSON.stringify，服务端语言也都有处理 JSON 的函数，使用 JSON 不会遇上什么麻烦
## 4.text/xml 
XML 作为编码方式的远程调用规范
# 3.小程序中API封装
## 1.api/config.js
因为wx.request不支持promise,所以在config.js中用promise封装wx.request
```javascript
const baseUrl = "http://localhost:3000" //这是我要请求的数据接口的公共部分
const request = (options) => {
  return new Promise((resolve, reject) => {
    wx.request({
      url: baseUrl + options.url,
      method: options.method || 'get',
      data: options.data || {},
      header: options.header || {
        'content-type': 'application/json'
      },
      success: resolve,
      fail: reject
    })
  })
}
export default request
```
## 2.api/http.js
```javascript
// 这里主要是对要请求的接口进行了封装，接口会有很多，放在同一个文件中方便管理
import request from 'config.js' //引入上面封装好的请求方法
// 获取flowerList的数据
const _getFlowerList = (data) => {
  return request({
    url: '/flowerList',
    data
  })
}

const _addTabData = (data)=>{
  return request({
    url: '/tabClassData',
    method: 'post',
    data,
    // headers: {}
  })
}
// 将方法导出，实现复用
export default {
  _getFlowerList,
  _addTabData
}
```
## 3.在小程序页面中使用
index.js
```javascript
import http from '../../api/http.js'
Page({
  // promise版
  // getFlowerList(){
  //   http._getFlowerList({
  //     _page: 1,
  //     _limit: 5
  //   })
  //   .then(res=>{
  //     console.log(res);
  //   })
  //   .catch(err=>{})
  // },

  // async版
  async getFlowerList() {
    let res = await http._getFlowerList({
      _page: 1,
      _limit: 5
    })
    console.log(res);
  },

  //添加
  async addTabData() {
    let res = await http._addTabData({
      title: '美食'
    })
    console.log(res);
  }

})
```

